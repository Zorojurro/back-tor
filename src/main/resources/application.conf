ktor {
    deployment {
        port = 8080
    }
    database {
        url = "jdbc:postgresql://localhost:5432/testdb"
        driver = "org.postgresql.Driver"
        user = "user"
        password = "password"
    }
}